# Runbook

D6 落实。预留 4 类剧本：

- `pii_audit.md`（季度 PII 漏脱审计）
- `supervisor_dead.md`（主管假死 → deputy 升级链）
- `pg_failover.md`（PG 主从切换 < 5s）
- `llm_provider_outage.md`（Provider 全失败 → 降级 + 飞书告警）
- `dual_run_diff_spike.md`（双跑差异飙升 → 回退预案）

D0 仅占位。

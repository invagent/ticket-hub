# 项目进度归档 — 2026-05-11

> **快照点**：D3-A 落地后  
> **分支**：`feat/d1-identity-routing`  
> **HEAD**：`69cc2fa feat(d3-stageA): agent_decisions 极简审计表 + classify Agent 同事务落表`  
> **总 commit 数**：44  
> **总迁移数**：7（`0001_d0_initial` → `0007_d3_a_agent_decisions`）  
> **总排期参考**：[`/Users/shaobin/.claude/plans/upgrade-plan-cheerful-kitten.md`](file:///Users/shaobin/.claude/plans/upgrade-plan-cheerful-kitten.md)

---

## 一、整体阶段进度

| 阶段 | 名称 | 计划起止 | 状态 | 备注 |
|---|---|---|---|---|
| **D0** | 仓库骨架 + SSO POC + 工具链 | 05-04 ~ 05-22 | ✅ 完成 | 提前完成 |
| **D1** | 身份与路由 + 代码迁移 + 双跑 | 05-25 ~ 07-03 | ✅ 完成 | 大幅提前 |
| **D2** | SLA 监控 + supervisor + zammad + 身份图谱 | 07-06 ~ 08-14 | ✅ 完成 | 大幅提前 |
| **D3** | Agent 全家桶 + PII + LLM Router | 08-17 ~ 10-23 | 🟡 进行中（A/B/C 完成） | 计划应 8 月才开工，实际已 5 月开工 |
| **D4** | 多模态 + How-To + cascade + Linear | 10-26 ~ 12-18 | ⬜ 未开工 | |
| **D5** | KSM 反向 + Internal Task + MCP | 12-21 ~ 01-22 | ⬜ 未开工 | |
| **D6** | feishu-python 退役 + 性能压测 + 切流 | 01-25 ~ 02-19 | ⬜ 未开工 | |
| **收尾** | 全量灰度观察 + 季度 PII 审计 | 02-22 ~ 03-19 | ⬜ 未开工 | |

> **进度评估**：当前实际进度大幅领先计划日历（领先 ~3 个月）。

---

## 二、已完成详细清单

### D0（commit `59e40fc`）
- 仓库骨架 75+ 文件、Backend Python 工具链、FastAPI + 飞书 SSO + admin
- Alembic `0001` 初版 schema、PII 单测覆盖率 98.1%
- Frontend Vite+React+TS+Tailwind、CLI Typer、docker-compose、CI、ADR-0001/0002

### D1（commits `edeed05` ~ `f25eb03`，stage A~K）
- ID 解析 / 路由 / 客户身份图谱 / Hub Issue 模型 / 50 条真实 KSM fixture
- 迁移 `0002_d1_business_tables`
- ADR-0005（SLA 升级链）

### D2（commits `e41afad` ~ `8c8faf9`，stage A~G2）
- zammad adapter+ingester、webhook 流量指标、materialized_metrics
- 产品线级 SLA 阈值、KSM 生产推送、用户 CRUD + 飞书 contact 同步
- 目录管理改造、admin 表单全面下拉化
- 迁移 `0003`/`0004`/`0005`、ADR-0012

### D3 已完成

| Stage | Commit | 内容 |
|---|---|---|
| **B** | `b55102b` | LLM Router + 智谱 GLM provider，真实 HTTP 跑通 |
| **C** | `7f7b1dd` | 工单分类 Agent — webhook → BackgroundTask → GLM → 写回 `tickets.predicted_*`；迁移 `0006` |
| **A** | `69cc2fa` | `agent_decisions` 极简审计表（迁移 `0007`） + `classify_ticket` 同事务落 `decision_type='classify_type'` |

### 测试与质量

- **397 个单测全绿**，整体覆盖率 **94.1%**
- **PII 模块覆盖率 98.1%**（达 D3 验收硬门槛 95%）
- ruff / mypy 在 D3-A 文件上全干净

---

## 三、D3 剩余待办（按重要度排序）

### 🔴 高优 — 验收门槛相关

1. **评测集 v1**（建议立刻开工）
   - 100 条人工标注（type / conflict / dedup 标准答案）
   - 路径：`backend/tests/eval/dataset_v1.jsonl`
   - 跑分脚本：`scripts/eval/run_eval.py`（D0 已 stub）
   - **作用**：验证 stage C classify 准确率是否真的 ≥ 90%；后续每个 Agent 上线门槛
   - **现状缺口**：classify 已上线但准确率未量化

2. **D3-D `conflict_detect` Agent**（决策 13）
   - Raw ticket 进来先看要不要拆（多模块/已有答案/在途重叠/都不命中 4 类）
   - 输出 `decision_type='split_ticket'` or `'no_split'` 写入 `agent_decisions`
   - 配套 prompt + 单测

3. **D3-E `dedup` Agent**（最重）
   - 向量召回 + LLM 判定 → `decision_type='dedup_link'` or `'dedup_new'`
   - **新基础设施需求**：pgvector 扩展 + embedding pipeline
   - 准确率门槛要靠评测集

### 🟡 中优 — 健壮性

4. **补 LLM provider**（OpenAI / DeepSeek / Anthropic）
   - 当前只有 GLM；fallback 链不完整
   - Router 已抽象，每家 ~80 行

5. **`split.py` 执行器**（无 LLM）
   - 把 `decision_type='split_ticket'` 的 proposal 落地为 Child tickets

### 🟢 低优 — 可见性

6. **`/admin/llm-cost` 前端页**
   - 从 `agent_decisions.proposal.cost_usd` 聚合
   - 按日期 / model / decision_type 分组

7. **`/admin/llm-config` 前端页**
   - Router 配置查看（Provider 顺序、可用 model）

---

## 四、本次刻意砍掉的（D3-A 简化决策）

| 砍掉项 | 理由 | 何时补回 |
|---|---|---|
| `agent_runs` 表（LLM 调用流水） | 已被 `llm_router_call_ok` 结构化日志覆盖 | D5 末做成本报表时再说 |
| `agent_decision_targets` 表 | 多 target 直接进 `proposal.related_ids` JSON | 若 99% 场景仍只关心 subject，不补 |
| `pii_maps` 表 + AES-GCM encryptor | 当前不发 PII 给外部 LLM？智谱 GLM 是国内合规 LLM，PII 风险等级低 | 接 OpenAI/Anthropic 之前必须补 |
| `classify.py` 里的 PII 脱敏 | 同上 | 接外部 LLM 前补 |

---

## 五、已知技术债

### 仓库 lint / mypy 债
- `app/api/webhooks.py`：mypy 17 个错（类型推断问题，主要在 `BackgroundTasks` 调用处）
- `app/services/users/feishu_sync.py`：2 个 `# noqa: BLE001` 失效注释
- `app/core/llm_router/__init__.py` / `providers/__init__.py`：`__all__` 未排序
- `tests/unit/core/test_llm_router.py`：5 个未使用 import + import 顺序
- `tests/unit/services/test_metrics_materializer.py`、`test_admin_catalog.py`：未使用 import
- `tests/unit/services/test_zammad_ingester.py`：2 个 `pytest.raises(match=...)` 正则未转义
- `tests/unit/adapters/test_glm_client.py`：`with` 嵌套可合并

> **总计 ~20 个 ruff 错** + 17 个 mypy 错。`make lint` 当前红灯，但单测全绿。
> 建议：D3-D 开工前花半天清债，否则后续提交都会被 CI 卡。

### 设计债
- **`HANDOFF.md` 严重过时**（写于 D0 时期，现已到 D3-A）
- **没有 `agent_runs` 表**意味着 cost / latency / tokens 数据只在日志里，无法 SQL 查询
- **PII 模块仍是 D0 骨架**：encryptor 是 Protocol（依赖注入），未实例化 AES-GCM；接 OpenAI 类外部 LLM 前必须补
- **`classify_v1` prompt 未做版本管理**：变更 prompt 后无法对比效果

### 流程债
- 评测集没建 → classify 准确率未知
- 灰度门槛（90% 准确率 / 10% 调整率）无法验证
- 没有任何回归基线

---

## 六、未来工作计划（D4 ~ 收尾）

### D4 多模态 + How-To + cascade + Linear（计划 8w）
- `services/agents/vision.py`（图/视频/PDF）
- `services/agents/how_to.py`（RAG）
- `services/agents/linear_match.py` + `adapters/linear`
- `services/cascade/reply_sync.py`（决策 15）
- `services/cascade/status_cascade.py`（决策 14）
- `services/knowledge/ingest.py` + `knowledge_chunks` 表（pgvector）
- `attachments` + `sync_outbox` 表
- 前端：`/hub-issues/` 4 类型分视图（核心运营页）+ `/admin/knowledge`
- **验收**：Vision ≥ 85%、How-To 采纳率 ≥ 50%、recall@5 ≥ 80%

### D5 KSM 反向 + Internal Task + MCP（计划 4w）
- KSM 反向写工单（仅状态/回复，不反向工单结构）
- Internal Task 联动飞书任务
- MCP Server 发布（Claude / Cursor / IDE 调用）
- **验收**：1 个 product_line 试运行 7d 无 P0/P1

### D6 feishu-python 退役 + 性能压测 + 切流（计划 4w）
- `feishu-python` 双跑差异 < 0.1%
- locust 压测达标
- 灰度切流：5% → 25% → 50% → 100%
- **验收**：每档 24h 无 P0/P1

### 收尾（计划 4w）
- 全量灰度观察
- 季度 PII 审计
- runbook 完善（5 类剧本）

---

## 七、ADR 索引

| 编号 | 主题 | 状态 |
|---|---|---|
| 0001 | 技术栈与 Monorepo | 已采纳 |
| 0002 | INT PK 而非 UUID | 已采纳 |
| 0005 | SLA 升级链：assignee → deputy → supervisor → 兜底池 | 已采纳 |
| 0012 | admin 权限边界与飞书用户同步策略 | 已采纳（飞书审批已生效） |

> 缺：D3 阶段决策（LLM Router 设计 / agent_decisions 极简化）尚未沉淀 ADR。建议下次提交补 `0013-llm-router-design.md` + `0014-agent-decisions-minimal.md`。

---

## 八、关键文件路径速查

```
ticket-hub/
├── HANDOFF.md                      ⚠️ 严重过时（D0 时期）
├── docs/progress/2026-05-11-status.md   ← 本文件
├── docs/spec/{data_model,api,routing}.md
├── docs/adr/{0001,0002,0005,0012}.md
├── backend/
│   ├── migrations/versions/
│   │   ├── 0001_d0_initial.py
│   │   ├── 0002_d1_business_tables.py
│   │   ├── 0003_d2_materialized_metrics.py
│   │   ├── 0004_d2_product_line_sla.py
│   │   ├── 0005_d2_catalog_modules_features.py
│   │   ├── 0006_d3_ticket_classification.py
│   │   └── 0007_d3_a_agent_decisions.py     ← 最新
│   ├── app/
│   │   ├── core/
│   │   │   ├── pii/                       ⚠️ D0 骨架（encryptor 未实现）
│   │   │   └── llm_router/
│   │   │       ├── router.py              ✅ D3-B
│   │   │       └── providers/glm.py       ✅ D3-B（仅 GLM）
│   │   ├── services/agents/
│   │   │   └── classify.py                ✅ D3-C + D3-A 落表
│   │   └── models.py                      含 AgentDecision (D3-A)
│   └── tests/
│       ├── eval/dataset_v1.jsonl          ⚠️ D0 stub，4 条占位
│       └── unit/                          397 测试，94.1% 覆盖率
└── scripts/eval/run_eval.py               ⚠️ D0 stub
```

---

## 九、立即可执行的下一步建议

按建议优先级：

1. **清 lint 债**（半天，必须）— 把 20 个 ruff 错和 17 个 mypy 错清掉，否则后续 PR 全卡
2. **建评测集 v1 雏形**（1 天）— 至少 30 条手标 + 跑通 `make eval`，量化 classify 当前准确率
3. **更新 HANDOFF.md**（10 分钟）— 让根目录的 handoff 文档不再误导
4. **D3-D conflict_detect**（2~3 天）— 装载评测集后开工，准确率有基线对比

---

**文档生成时间**：2026-05-11  
**生成方式**：归档当前进度 + 待办 + 未来计划至此快照

# 文件清理建议

- 生成日期：2026-06-23
- 范围：主仓库 `/Users/shaobin/Documents/ticket-hub` 工作区
- 依据：实际审计（git 跟踪状态 + 磁盘占用 + 引用关系），非泛泛建议
- 说明：每条标注 🔴高/🟡中/🟢低 优先级，给出确切命令；**破坏性操作前自行确认**

---

## 🔴 高优先（风险 / 安全）

### 1. `sample/`（第三方 fork）— 308 MB，未跟踪未忽略，含泄露邮箱

| 项 | 现状 |
|---|---|
| git 状态 | `?? sample/`（**未跟踪、也未 gitignore**）|
| 体积 | 308 MB（`.git` 956K + `backend/.venv` 115M + `frontend/node_modules` 189M）|
| 风险 1 | 一次 `git add .` 就会把 308MB + 整个第三方仓库误提交进主仓 |
| 风险 2 | 仍含已脱敏前的明文（`shaobin_chen@kingdee.com` 等），随主仓扩散 |

**建议（二选一）：**

```bash
# 方案 A（推荐）：分析已完成，直接移出仓库工作区
mv sample /Users/shaobin/Documents/ticket-hub-sample-fork   # 移到仓库外保留
# 或确认不再需要后删除：rm -rf sample

# 方案 B：暂时保留但杜绝误提交 —— 加入 .gitignore
echo "sample/" >> .gitignore
```

> 注：`sample/` 本身是别人的 git 仓库（remote 指向 `xiaojianjian2233/ticket`），
> 不该嵌在主仓工作区里。分析报告已存 `docs/analysis/2026-06-23-sample-fork-diff.md`，
> 移除/删除 `sample/` 不丢分析结论。

### 2. 凭证轮换清单（安全延续）

本轮脱敏只是**止血**——以下值已在 git 历史 / 已 fork 到第三方 / 在本对话明文出现过，
属「已泄露」，建议评估轮换（具体值见 `CLAUDE.local.md`，此处不复制）：

| 凭证 | 状态 | 建议 |
|---|---|---|
| 生产服务器 root 口令 | 对话中明文出现 | 改口令 / 换为密钥登录 + 禁用密码登录 |
| 生产服务器 IP | CLAUDE.md 历史明文 + 已 fork | 收紧安全组/防火墙（限源 IP），防端口扫描 |
| DashScope / Vision API key | 对话中明文出现 | 阿里云控制台轮换，更新服务器 `.env` |
| Linear 个人 API key | 对话中明文出现 | Linear 设置页 revoke 重建，更新 `.env` |
| 员工邮箱（minjun_gong / shaobin_chen）| 已脱敏，无需轮换 | 仅提醒：测试 fixture 今后用 `@example.com` |

---

## 🟡 中优先（卫生）

### 3. 旧进度文档 `docs/progress/2026-05-11-status.md`

- 已被 `2026-06-12-plan.md` 取代，但仍被 `README.md` 引用。
- **建议**：更新 `README.md` 指向新计划，老文档加「⚠️ 已归档，最新见 2026-06-12-plan.md」头部，或移入 `docs/progress/archive/`。不必删（历史留痕有价值）。

### 4. 空的 service 占位目录（5 个）

物理存在但**无任何跟踪文件**（git 看不见，纯本地空壳）：
`backend/app/services/{handlers,knowledge,notify,sync,workflows}`

- **建议**：要么删掉（`rmdir` 这些空目录），要么各放一个 `__init__.py` + 注释说明规划用途，避免后来人困惑「为什么有空目录」。

---

## 🟢 低优先（本地清理，不影响 git，已正确 gitignore）

### 5. 构建/缓存产物

| 目录 | 体积 | gitignore |
|---|---|---|
| `backend/.mypy_cache` | 22 MB | ✅ 已忽略 |
| `backend/.ruff_cache` | 120 K | ✅ 已忽略 |
| `backend/htmlcov` | — | ✅ |
| `__pycache__`（75 个）| — | ✅ |

```bash
cd backend && make clean   # 一键清 .venv/缓存/覆盖率；或仅删缓存：
find . -name __pycache__ -type d -prune -exec rm -rf {} +
```

### 6. `.DS_Store`（6 个，macOS 产物）

已 gitignore（0 个被跟踪），不影响仓库，可本地删：

```bash
find . -name .DS_Store -not -path "*/.git/*" -delete
```

---

## 一页纸执行顺序

```bash
# 1) 高：杜绝 sample/ 误提交（先确认分析已留档）
echo "sample/" >> .gitignore        # 或 mv sample 出仓库

# 2) 中：归档旧文档（手动改 README + 文档头）
# 3) 中：清理空 service 目录（手动 rmdir）

# 4) 低：本地清缓存
cd backend && make clean
find /Users/shaobin/Documents/ticket-hub -name .DS_Store -not -path "*/.git/*" -delete

# 5) 安全：按 §2 清单逐项轮换凭证（控制台操作，非本仓）
```

> 本文件（`文件清理建议.md`）本身是一次性操作清单，执行完可删除，不必长期留在仓库。
